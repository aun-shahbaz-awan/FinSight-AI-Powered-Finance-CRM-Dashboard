{
  "name": "finsight-ai",
  "private": true,
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "db:generate": "pnpm --filter @finsight/database db:generate",
    "db:migrate": "pnpm --filter @finsight/database db:migrate",
    "db:studio": "pnpm --filter @finsight/database db:studio"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}